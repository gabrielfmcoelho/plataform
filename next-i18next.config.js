module.exports = {
    i18n: {
      locales: ['en', 'pt-BR'],  // Add all supported locales
      defaultLocale: 'pt-BR',     // Set the default locale
    },
    react: { useSuspense: false }, // Disable Suspense if using server-side rendering
  };
  